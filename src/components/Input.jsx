import styled from "styled-components";

const Input = styled.input`
  color: #282c34;
  font-size: 1em;
  border-radius: 3px;
  display: inline-block;
  padding: 10px;
  margin: 0px;
  border: "2px solid #da7b93";
  font-family: "Roboto", sans-serif;
  //width: ${props => (props.primary ? "12rem" : "7rem")};
`;

export default Input;
